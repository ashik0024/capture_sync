import 'package:flutter/material.dart';

/// Dark theme palette shared across the Upload Manager screens/widgets.
class UploadPalette {
  UploadPalette._();

  static const bg = Color(0xFF0C1220);
  static const card = Color(0xFF161D30);
  static const cardBorder = Color(0xFF232B42);
  static const blue = Color(0xFF3D5CFF);
  static const green = Color(0xFF3DDC84);
  static const red = Color(0xFFFF5C5C);
  static const orange = Color(0xFFFFB03D);
  static const textPrimary = Colors.white;
  static const textSecondary = Color(0xFF8993AC);
}